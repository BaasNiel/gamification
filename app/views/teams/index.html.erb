<p id="notice"><%= notice %></p>

<h1>Teams</h1>

<table border=1>
  <thead>
    <tr>
      <th>Name</th>
      <th>Avatar</th>
      <th>Start of week</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @teams.each do |team| %>
      <tr>
        <td><%= team.name %></td>
        <td><%= team.avatar %></td>
        <td><%= team.start_of_week %></td>
        <td><%= link_to 'Show', team %></td>
        <td><%= link_to 'Edit', edit_team_path(team) %></td>
        <td><%= link_to 'Destroy', team, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Team', new_team_path %>
